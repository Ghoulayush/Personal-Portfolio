import type { MusicConfig } from "@/types/music";

// Albums and artists are populated only with real entries the owner provides.
export const music: MusicConfig = {
  featuredAlbumId: undefined,
  albums: [],
  artists: [],
  note: "",
};
